#include<iostream>
#include<vector>
#include"adminMenu.h"

using namespace std;

int main()
{
     book b;
     book_database db;
     /* b.new_book();
     db.upload(b); */ 
    /*   for(int i=0;i<5;i++)
    {
        b.new_book();
        db.upload(b); 
    }  */ 
        /* char a[100];
    strcpy(a,"Kolkata");

    vector<book> info=db.download();
    cout<<db.extract(info);    
     */
    // db.deleteRecord(1);
    //db.upload(2);
     admin_menu ad;
     ad.menu(); 
    //


}